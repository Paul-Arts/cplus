    #include <vector> //#
    #include <algorithm> //#
    using namespace std; //#
    void sortDataCont(vector<int> &data) {
        // ...exemplarisch...
        sort(data.begin(), data.end());
    }
    void sortDataIt(vector<int>::iterator b, vector<int>::iterator e) {
        // ...exemplarisch...
        sort(b, e);
    }

